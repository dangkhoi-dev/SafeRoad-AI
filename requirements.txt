# Cài đặt: pip install -e .   (khuyến nghị — dùng pyproject.toml)
# File này liệt kê phụ thuộc trực tiếp cho môi trường không dùng pip install -e.
numpy>=1.24
opencv-python>=4.8
ultralytics>=8.3
torch>=2.0
scipy>=1.10
scikit-learn>=1.3
joblib>=1.3
fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
jinja2>=3.1
python-multipart>=0.0.9

# Phát triển
pytest>=7.4
